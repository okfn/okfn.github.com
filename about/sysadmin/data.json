---
---

{
  "sysadmins":
  { {% for person in site.categories.members %}{% if person.roles contains "sysadmin" %}
    "{{ person.username }}":
    {
      "name": "{{ person.title }}",
      "key": "{{ person.publickey }}"
    } {% if forloop.last != true %},{% endif %}{% endif %}{% endfor %}
  },
  "services":
    { {% for machine in site.data.machines %}
      "{{ machine.name }}":
      { {% for service in machine.services %}
	 "name": "{{ service.name }}",
	 "url": "{{ service.url }}"
      }{% if forloop.last != true %},{% endif %}{% endfor %}{% if forloop.last != true %},{% endif %}{% endfor %}

    }
}
